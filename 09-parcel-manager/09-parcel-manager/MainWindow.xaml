<Window x:Class="_09_parcel_manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_09_parcel_manager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TITLE -->

        <TextBlock
            Grid.Row="0"
            Text="Parcel Manager"
            FontSize="26"
            FontWeight="Bold"/>


        <!-- MASTER ROOT -->

        <TextBlock
            Grid.Row="1"
            Text="Master Root Folder"
            Margin="0,25,0,5"/>

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox
                Grid.Column="0"
                x:Name="MasterFolderTextBox"
                Height="30"
                IsReadOnly="True"/>

            <Button
                Grid.Column="1"
                Content="Browse"
                Width="80"
                Height="30"
                Margin="8,0,0,0"
                Click="BrowseFolder_Click"/>

        </Grid>


        <!-- MASTER -->

        <TextBlock
            Grid.Row="4"
            Text="Master Drawing"
            FontSize="18"
            FontWeight="Bold"/>

        <Button
            Grid.Row="5"
            Content="MASTER.dwg"
            Width="150"
            Height="40"
            HorizontalAlignment="Left"
            Margin="0,10,0,0"
            Click="OpenMasterFile_Click"/>

    </Grid>
</Window>
